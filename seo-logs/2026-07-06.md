# Rapport SEO quotidien — 2026-07-06 (lundi)

## Programme du jour
Lundi → génération et publication d'1 article de blog SEO.

## ⚠️ Problème bloquant : clé ANTHROPIC_API_KEY invalide
La clé `ANTHROPIC_API_KEY` fournie dans les instructions de la routine est **rejetée par l'API Anthropic** (`authentication_error: invalid x-api-key`), vérifié par appel direct à `api.anthropic.com`. Résultat : le script `scripts/seo-daily.mjs` (qui appelle l'API Claude en direct pour générer l'article) plante avec une erreur réseau (`ECONNRESET`) au lieu de générer le contenu.

**Action requise : remplacer `ANTHROPIC_API_KEY` par une clé valide** pour que l'automatisation `node scripts/seo-daily.mjs` fonctionne seule à l'avenir (utile notamment si cette routine tourne un jour via GitHub Actions, sans agent Claude Code pour compenser).

Par ailleurs, l'accès direct à l'API Sanity (`t3ow1rmc.api.sanity.io`) depuis ce environnement est bloqué par la politique réseau du sandbox (403 au niveau du proxy egress) — comportement attendu, prévu par le script (`[SANITY_MCP_REQUIRED]`), contourné ici via le connecteur MCP Sanity.

## Ce qui a été fait malgré tout
Comme la génération de contenu ne pouvait pas passer par le script (clé invalide), l'article a été rédigé directement par l'agent Claude Code, en suivant strictement le prompt/schema du script `seo-blog.mjs` (structure, longueur, FAQ, contraintes SEO, règle images catalogue uniquement), puis publié via le connecteur MCP Sanity.

- **Article publié** : "BYD Seal U DM-i en Afrique de l'Ouest : prix FCFA, autonomie et avis 2026"
  - Slug : `byd-seal-u-dmi-afrique-ouest-prix-fcfa-autonomie-avis-2026`
  - Catégorie : Guides d'achat
  - ~1 823 mots (minimum 1 500 requis) + FAQ de 4 questions
  - seoTitle (56 car.) et seoDescription (157 car.) dans les bornes cibles
  - 2 images + 1 image de couverture, toutes issues de vraies photos catalogue du modèle BYD Seal U DM-i (aucune image générique)
  - Sujet vérifié non dupliqué parmi les 34 articles déjà publiés
  - Spécifications techniques (batterie 18,3/26,6 kWh, autonomie WLTP, recharge DC 18 kW) vérifiées via recherche web avant rédaction
  - Publié en direct (pas en brouillon) : `_id` `6568e52d-0e4a-4cfd-8dce-accdb85be659`

## Voitures optimisées (SEO)
Aucune — hors programme du lundi (prévu mardi/jeudi/samedi).

## Maillage interne
Aucun — hors programme du lundi (prévu dimanche).

## Erreurs
- `ANTHROPIC_API_KEY` invalide (voir ci-dessus) — à corriger avant la prochaine exécution automatisée du script.
- Aucune variable `VERCEL_DEPLOY_HOOK` fournie — pas de redéploiement Vercel déclenché (le contenu du blog étant servi depuis Sanity, l'article devrait être visible sans rebuild ; à vérifier si le site utilise un cache statique).
